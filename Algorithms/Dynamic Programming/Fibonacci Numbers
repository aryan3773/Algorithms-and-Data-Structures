import time
def fib(x,d):
    if x in d:
        return d[x]
    else:
        ans = fib(x-1,d) + fib(x-2,d)
        d[x] = ans
        return ans
    
d={1:1, 2:1, 3:2}
print(fib(46,d))

def fibo(x):
    if x==0:
        return 0
    elif x==1 or x==2:
        return 1
    else:
        return fibo(x-1) + fibo(x-2)
